sudo apt update
sudo apt install -y docker.io
sudo systemctl start docker
sudo systemctl enable docker


sudo lsof -i :80

sudo docker rm -f portfolio-webapp 2>/dev/null || true





sudo systemctl stop nginx
sudo systemctl disable nginx

sudo docker rm -f portfolio-webapp



docker build -t portfolio-webapp:latest .

docker save -o portfolio-webapp.tar portfolio-webapp:latest

sudo docker run -d --name portfolio-webapp -p 8080:80 portfolio-webapp:latest

http://localhost:8080
http://127.0.0.1:8080



